using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MakeRecipe : MonoBehaviour
{

    [SerializeField] private Ingredient _secretIngredient;
    [SerializeField] private Ingredient _secretIngredient2;
    [SerializeField] private List<Ingredient> _recipe;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
